{% extends "base.html.twig" %}
{% block title 'Login' %}
{% block body %}
    <div class="container">
        <h1>Connexion :</h1>
        <form action="{{ path('security_login') }}" method="post">
            <div class="form-group">
                <label><b>Email :</b></label>
                <input type="email" placeholder="Entrer un email valide" name="_username" required class="form-control">
            </div>
            <div class="form-group">
                <label><b>Mot de passe :</b></label>
                <input type="password" placeholder="Entrer un mot de passe valide" name="_password" required class="form-control">
            </div>
            <button type="submit" class="btn btn-primary">Connexion</button>
        </form>

        <div class="container">
        <span>Vous n'êtes pas inscrit ? <a
                    href="{{ path('security_registration') }}"> Cliquez ici</a></span>
        </div>
    </div>
{% endblock %}